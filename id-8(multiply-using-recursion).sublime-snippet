<snippet>
    <content><![CDATA[ll mul(ll a,ll b){
    if(b==0){
        return 0;
    }
    if(b>0){
    return (a+mul(a,b-1));  
    }
    else{
    return (-a+mul(a,b+1));  
        
    }

}]]></content>
    <!-- Optional: Set a tabTrigger to define how to trigger the snippet -->
    <tabTrigger>id-8(multiply-using-recursion)</tabTrigger>
    <!-- Optional: Set a scope to limit where the snippet will trigger -->
    <!-- <scope>source.python</scope> -->
</snippet>
